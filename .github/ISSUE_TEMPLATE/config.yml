# Keep the "Open a blank issue" option available alongside the structured forms.
blank_issues_enabled: true
# No external contact links are configured. Maintainers can add entries here
# later (each needs name, url, and about); until then, none are advertised.
